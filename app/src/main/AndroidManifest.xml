<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:extractNativeLibs="true"
        android:theme="@style/Theme.TrueMark">
        
        <activity android:name=".SplashActivity" android:exported="true" android:theme="@style/Theme.TrueMark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Auth & Home -->
        <activity android:name=".LoginActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".RegisterActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".ForgotPasswordActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".AdminHomeActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".EditProfileActivity" android:windowSoftInputMode="adjustResize" />

        <!-- Admin Features -->
        <activity android:name=".AdminAddUserActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".AdminViewUsersActivity" />
        <activity android:name=".AdminViewStudentsActivity" />
        <activity android:name=".AdminManageAdminsActivity" />
        <activity android:name=".AdminAddAdminActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".AdminAddStudentDetailsActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".UserDetailsActivity" />

        <!-- Attendance & Session -->
        <activity android:name=".FacultyAttendanceActivity" />
        <activity android:name=".FacultyAttendanceGroupsActivity" />
        <activity android:name=".ManualAttendanceActivity" />
        <activity android:name=".FacultyGroupStudentsActivity" />
        <activity android:name=".FacultyStartSessionActivity" />
        <activity android:name=".FacultySessionActivity" />
        <activity android:name=".StudentAttendanceActivity" />
        <activity android:name=".AttendanceHistoryActivity" />
        <activity android:name=".SubjectAttendanceDetailActivity" />
        <activity android:name=".TotalAttendanceActivity" />
        <activity android:name=".UserAnalyticsActivity" />

        <!-- QR Attendance -->
        <activity android:name=".StudentScannerActivity" />
        <activity android:name=".AttendanceAnalyticsActivity" />
        
        <!-- Messaging Features -->
        <activity android:name=".ChatListActivity" />
        <activity android:name=".ChatActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".CreateGroupActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".GroupDetailsActivity" />
        <activity android:name=".ViewMediaActivity" />
        <activity android:name=".NotificationActivity" />
        
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>